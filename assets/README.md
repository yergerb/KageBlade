# Assets

Game art lives here.

- `sprites/`: character sprite sheets and frame exports
- `effects/`: slash trails, sparks, block bursts, parry flashes
- `backgrounds/`: arenas and parallax layers
- `ui/`: health bars, icons, menus, portraits

Keep source sheets when they are useful for re-exporting, but game code should load optimized transparent PNGs or packed atlases.
